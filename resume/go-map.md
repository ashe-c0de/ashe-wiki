桶（bucket）数组
h1 = hash(key1)
index = h1 % （2^B - 1）

哈希冲突/索引重合
	拉链法
		单个桶最多存 8 个键值对，如果一个 bucket 满了，会链表连接一个溢出桶。
		
增量扩容
	写入数据时判断负载因子是否过高（超过6.5），会触发扩容，创建一个新的 buckets 并逐步迁移数据（渐进式扩容，避免一次性迁移导致 STW 时间过长）
等量扩容
	负载因子不高，但溢出桶过多（内存碎片化）

loadfactor（负载因子） = count（键值对数量） / 2^B（桶数量）
