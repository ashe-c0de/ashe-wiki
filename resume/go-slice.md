← [Ashe wiki](../README.md)

---
Go 的 slice 底层是一个包含三字段的结构体：

- 指向底层数组的指针
- 当前长度（Len）
- 容量（Cap）

当 append 导致 len > cap 时，Go 会分配一个新的底层数组，并将原数据复制过去。扩容策略大致如下（具体实现可能随版本优化）：  
如果原容量 < 1024，新容量 ≈ 原容量 * 2  
如果原容量 ≥ 1024，新容量 ≈ 原容量 * 1.25
